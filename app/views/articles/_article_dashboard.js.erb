$('#articles').append("<%= escape_javascript(render 'articles')%>");
$('#paginator').html("<%= escape_javascript(link_to_next_page(@articles, 'LOAD MORE', remote: true, id: 'load_more'))%>");
if (!$('#load_more').length) { $('#paginator').remove(); }