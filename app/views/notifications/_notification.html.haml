%li
  = link_to "#{notification.notified_by.first_name} has #{notification.notice_type}ed on your article", link_through_path(notification)
  %li.divider{ role: 'separator' }
    %li.text-center= link_to 'View All', notifications_path
